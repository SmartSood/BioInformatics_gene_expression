# DepMap Backend API Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: depmap-backend-api
  namespace: gene-web
  labels:
    app: depmap-backend
    component: api
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: depmap-backend
      component: api
  template:
    metadata:
      labels:
        app: depmap-backend
        component: api
    spec:
      containers:
      - name: depmap-backend
        image: docker-registry.example.com/gene-depmap:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8001
          name: http
        envFrom:
        - configMapRef:
            name: shared-config
        - configMapRef:
            name: depmap-backend-config
        env:
        - name: S3_BUCKET
          valueFrom:
            secretKeyRef:
              name: aws-credentials
              key: S3_BUCKET
        - name: S3_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: aws-credentials
              key: S3_ACCESS_KEY
        - name: S3_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: aws-credentials
              key: S3_SECRET_KEY
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: redis-credentials
              key: REDIS_URL
        resources:
          requests:
            cpu: 500m
            memory: 2Gi
          limits:
            cpu: 2000m
            memory: 8Gi
        livenessProbe:
          httpGet:
            path: /docs
            port: 8001
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /docs
            port: 8001
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 2
---
# DepMap Backend API Service
apiVersion: v1
kind: Service
metadata:
  name: depmap-backend
  namespace: gene-web
  labels:
    app: depmap-backend
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
  selector:
    app: depmap-backend
    component: api
---
# DepMap Backend Worker Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: depmap-backend-worker
  namespace: gene-web
  labels:
    app: depmap-backend
    component: worker
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: depmap-backend
      component: worker
  template:
    metadata:
      labels:
        app: depmap-backend
        component: worker
    spec:
      containers:
      - name: depmap-backend-worker
        image: docker-registry.example.com/gene-depmap:latest
        imagePullPolicy: Always
        command: ["python", "-m", "rq.cli", "worker"]
        envFrom:
        - configMapRef:
            name: shared-config
        env:
        - name: S3_BUCKET
          valueFrom:
            secretKeyRef:
              name: aws-credentials
              key: S3_BUCKET
        - name: S3_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: aws-credentials
              key: S3_ACCESS_KEY
        - name: S3_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: aws-credentials
              key: S3_SECRET_KEY
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: redis-credentials
              key: REDIS_URL
        resources:
          requests:
            cpu: 500m
            memory: 2Gi
          limits:
            cpu: 2000m
            memory: 8Gi
---
# DepMap Backend HPA
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: depmap-backend-api-hpa
  namespace: gene-web
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: depmap-backend-api
  minReplicas: 2
  maxReplicas: 4
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
